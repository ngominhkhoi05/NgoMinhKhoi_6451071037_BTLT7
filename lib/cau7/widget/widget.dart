import 'package:flutter/material.dart';

class Cau7Widget extends StatelessWidget {
  const Cau7Widget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(child: Padding(padding: EdgeInsets.all(16), child: Text('Câu 7 Widget')));
  }
}
