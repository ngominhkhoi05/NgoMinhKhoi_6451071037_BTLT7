import 'package:flutter/material.dart';

class Cau4Widget extends StatelessWidget {
  const Cau4Widget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(child: Padding(padding: EdgeInsets.all(16), child: Text('Câu 4 Widget')));
  }
}
