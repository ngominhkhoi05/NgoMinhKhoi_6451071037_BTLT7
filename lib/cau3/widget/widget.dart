import 'package:flutter/material.dart';

class Cau3Widget extends StatelessWidget {
  const Cau3Widget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(child: Padding(padding: EdgeInsets.all(16), child: Text('Câu 3 Widget')));
  }
}
